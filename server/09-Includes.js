/*El método includes determina si un array o string incluye un determinado elemento. Devuelve true o false, si existe o no respectivamente.*/

/*Arreglo que se construye para el ejemplo*/
const pets = ["cat","dog","bat"];

/*Utilizamos includes con el fin de buscar el elemento cat, en este caso como si se encuentra retorna true*/
const ejemploIncludes = pets.includes("cat");

/*Imprimimos por consola el resultado del ejemplo anterior*/
console.log(ejemploIncludes);